/*
 * generated by Xtext 2.13.0
 */
package com.dml.dsl.formatting2

import dataModelPackage.DataModel
import dataModelPackage.DataPackage
import dataModelPackage.Parameter
import org.eclipse.xtext.formatting2.AbstractFormatter2
import org.eclipse.xtext.formatting2.IFormattableDocument

class DmlFormatter extends AbstractFormatter2 {
	
//	@Inject extension DmlGrammarAccess

	def dispatch void format(DataPackage dataPackage, extension IFormattableDocument document) {
		// TODO: format HiddenRegions around keywords, attributes, cross references, etc. 
		for (DataModel dataModel : dataPackage.getDataModelCollections()) {
			dataModel.format;
		}
	}

	def dispatch void format(DataModel dataModel, extension IFormattableDocument document) {
		// TODO: format HiddenRegions around keywords, attributes, cross references, etc. 
		for (Parameter parameter : dataModel.getPrimitives()) {
			parameter.format;
		}
	}
	
	// TODO: implement for Parameter, AbstractType, ArrayType, PrimitiveType, ArrayValue
}
